name: darkauth
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - bcrypt
  - numpy
  - scikit-learn
  - flask
  - pytest
  - selenium
  - nodejs
  - pip:
      - twilio
      - passlib
      - matplotlib
      - seaborn
